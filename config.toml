name = "marix"

[runtime]
environment = "development"
mode = "network"
marix_path = "."

[client]
interactive = true
request_timeout_ms = 30000

[server]
enabled = true
ip = "{{SERVER_IP}}"
auth_token = "{{SERVER_AUTH_TOKEN}}"
client_port = {{SERVER_PORT_CLIENT}}
host_port = {{SERVER_PORT_HOST}}
telemetry_port = {{SERVER_PORT_TELEMETRY}}
telemetry_http_port = {{SERVER_PORT_TELEMETRY_HTTP}}
max_turns = 32

[model]
selected = "deepseek"

[model.deepseek]
endpoint = "https://api.deepseek.com/chat/completions"
model = "deepseek-v4-flash"
api_key = "{{MODEL_DEEPSEEK_API_KEY}}"

[model.glm]
endpoint = "https://open.bigmodel.cn/api/paas/v4/chat/completions"
model = "glm-4.7-flash"
api_key = "{{MODEL_GLM_API_KEY}}"

[tool]
directory = "tool"
