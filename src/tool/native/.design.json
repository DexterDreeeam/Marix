{
  "schemaVersion": 1,
  "module": {
    "path": "src/tool/native",
    "name": "native",
    "purpose": "Groups the concrete native tool programs by category. file/ owns filesystem tools (read, write, list, search); shell/ owns native shell command execution. mod.rs is re-export wiring only.",
    "changeStatus": "unchanged"
  },
  "childModules": [
    {
      "path": "src/tool/native/file",
      "name": "file",
      "purpose": "Filesystem native tool programs: ReadFile, WriteFile, ListDirectory, and SearchText."
    },
    {
      "path": "src/tool/native/shell",
      "name": "shell",
      "purpose": "Native shell command execution tool program: ShellExecute."
    }
  ],
  "elements": []
}
