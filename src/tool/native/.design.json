{
  "schemaVersion": 1,
  "module": {
    "path": "src/tool/native",
    "name": "native",
    "purpose": "Groups the concrete native tool programs by category. file/ owns filesystem tools (read, write, list, search); shell/ owns native shell command execution. Native previews now wrap schema constants in typed ToolInputSchema/ToolOutputSchema content wrappers before returning ToolPreview, and invoke() implementations perform real filesystem/shell IO and return JSON results.",
    "changeStatus": "unchanged"
  },
  "childModules": [
    {
      "path": "src/tool/native/file",
      "name": "file",
      "purpose": "Filesystem native tool programs: ReadFile, WriteFile, ListDirectory, and SearchText, each wrapping preview schema constants in ToolInputSchema/ToolOutputSchema content and implementing invoke() with std::fs work."
    },
    {
      "path": "src/tool/native/shell",
      "name": "shell",
      "purpose": "Native shell command execution tool program: ShellExecute, wrapping preview schema constants in ToolInputSchema/ToolOutputSchema content and running commands through std::process::Command."
    }
  ],
  "elements": []
}
