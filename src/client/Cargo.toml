[package]
name = "marix-client"
version = "0.1.0"
edition = "2024"

[dependencies]
marix-agent = { path = "../agent" }
marix-common = { path = "../common" }

[lib]
name = "marix_client"
path = "lib.rs"
