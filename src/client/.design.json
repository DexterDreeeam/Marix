{
  "schemaVersion": 1,
  "module": {
    "path": "src/client",
    "name": "client",
    "purpose": "Client-facing draft split into an interactive streaming CLI binary/interface with mod.rs-only module wiring and a remoc-backed core client session/task endpoint that sends each prompt request, receives streamed response message segments, and separates public client interfaces from private transport helpers with explicit Rust section markers where needed.",
    "changeStatus": "modified"
  },
  "modified": [
    "."
  ],
  "childModules": [
    {
      "path": "src/client/core",
      "name": "core",
      "purpose": "Core client session endpoint backed by remoc/TCP with task creation, initial request submission, and streamed response segment receives."
    },
    {
      "path": "src/client/cli",
      "name": "cli",
      "purpose": "Interactive CLI binary entry and public interface for connecting to the core client endpoint, sending each typed prompt as a chat request, streaming response segments to stdout, and reporting how many segments were printed, with mod.rs limited to module wiring."
    }
  ],
  "elements": []
}
