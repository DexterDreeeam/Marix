Select one to five mutually independent available native tools that complete the current goal using the supplied context. Use native tool calls only and return no text. Every arguments object must match the selected tool schema.
