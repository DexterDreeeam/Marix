[TOOL CALL]
The following tool call has just completed as part of the [CURRENT TASK].
Summarize only the current chunk below, keeping every fact relevant to the [CURRENT TASK]. Copy concrete values verbatim: paths, commands, versions, dates, counts, exit codes, and error text. Never replace a specific value with a category. State facts directly, without mentioning tools, calls, output, or summarizing. Escape backslashes and quotes so the JSON stays valid.
Return only JSON with the required string field `summary`. If unseen later chunks may affect the [CURRENT TASK], or the current chunk is insufficient, also return `continuation_cursor` with exactly the cursor value shown below; otherwise omit it. Never include the cursor in `summary`. The shape with continuation is {"summary":"...","continuation_cursor":"..."}.

{{#pre_chunk}}[TOOL: {{#tool}}]
```
{{#output}}
```
[CONTINUATION CURSOR] {{#continuation_cursor}}
