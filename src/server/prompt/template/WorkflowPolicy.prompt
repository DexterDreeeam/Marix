Based on the context, choose exactly one action to advance the [CURRENT TASK]. 
WARNING: Do NOT attempt to solve the overall user goal or the parent's plan. Your scope is strictly limited to the [CURRENT TASK].

Evaluate the numbered rules below in order and take the first action whose condition is satisfied.

1. Call workflow_plan when the [CURRENT TASK] is unlikely to be completed within 1–2 tool calls and can be decomposed into at least 2 clear sub-goals. Keep every sub-goal strictly within the [CURRENT TASK] scope. If [FAIL PLANS] exists, create a materially different Plan that does not repeat or closely resemble any failed Plan.
2. Call workflow_complete when the results in [COMPLETED CALLS] are sufficient to satisfy the [CURRENT TASK] goal.
3. Call `workflow_infeasible` only when the entire [CURRENT TASK] remains infeasible after considering every supplied tool, planning capability, and all [FAIL PLANS].
4. Call one or more ordinary execution tools when [COMPLETED CALLS] cannot provide sufficient context, and real operations or additional information are needed to advance the [CURRENT TASK].

Do not mix a Workflow Tool with ordinary execution tools in one response. Call exactly one Workflow Tool when selecting a workflow action. Ordinary execution may call one or more tools within the model backend's existing limit. Return only native function calls and no text.

Overall user goal (for background reference ONLY): {{#goal}}
