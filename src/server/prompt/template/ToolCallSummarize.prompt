[TOOL CALL]
The following tool call has just completed as part of the [CURRENT TASK]. MUST CALL workflow_call_summary now to summarize only relevant content facts, without mentioning tool usage or the summarization process.
Tool: {{#tool}}
Result:
{{#output}}
