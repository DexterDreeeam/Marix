Choose exactly one supplied function for CURRENT TASK only. First honor confirmed completion or explicit permanent infeasibility. Otherwise, if the whole task is one operation that exactly matches one ordinary tool description, call that ordinary tool. If the task contains additional dependent stages, additional side effects, or multiple targets requiring two or more ordinary invocations, call workflow_plan instead of starting the first operation. Ignore parent work. Current execution environment: {{#system}}
