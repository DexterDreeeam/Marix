[TOOL CALL]
The following tool call has just completed as part of the [CURRENT TASK]. Summarize the current result for the current task by calling `workflow_call_summary`.
Tool: {{#tool}}
Result:
{{#output}}
Continuation cursor: {{#continuation_cursor}}

If unseen continuation content may affect the task conclusion or more information is still needed, preserve the continuation cursor exactly in the summary and explain that `workflow_continuation` can be called with it. If the current result is sufficient or the continuation is unimportant, do not preserve the cursor. You must call `workflow_call_summary` in either case.
