{
  "schemaVersion": 1,
  "module": {
    "path": "src",
    "name": "src",
    "purpose": "Rust workspace source root for Marix crates with flat Cargo crate layout. It organizes reusable common config/external re-export/logging/protocol/structure modules, agent model/session/task orchestration, client session/event surfaces, and host-side tool execution stubs while Cargo manifests define crate wiring.",
    "changeStatus": "unchanged"
  },
  "childModules": [
    {
      "path": "src/agent",
      "name": "agent",
      "purpose": "Agent crate namespace for model backend abstractions, Session and SessionContext runtime orchestration, and split task folder interfaces."
    },
    {
      "path": "src/client",
      "name": "client",
      "purpose": "Client crate namespace for user-facing session commands, client events, and channel endpoints that communicate with the agent through shared SessionEvent values."
    },
    {
      "path": "src/common",
      "name": "common",
      "purpose": "Shared common crate namespace with reusable config, crate-local external re-export adapters, logging, protocol models, and structural data/channel interfaces."
    },
    {
      "path": "src/host",
      "name": "host",
      "purpose": "Host crate namespace for agent channel connection stubs and an Executor-owned tool preview, execution, status-query, cancel, kill, and update interface."
    }
  ],
  "elements": []
}
