{
  "schemaVersion": 1,
  "module": {
    "path": "src",
    "name": "src",
    "purpose": "Rust workspace source root for Marix crates with a flat Cargo crate layout plus prompt-template sources. It organizes agent, client, common, prompt, host, protocol, and tool crates/modules; the agent crate now includes an agent-side plan queue module and TaskState integration that stores PlanQueue-backed plan records so follow-up plans begin only after all stored steps for a plan complete.",
    "changeStatus": "unchanged"
  },
  "childModules": [
    {
      "path": "src/agent",
      "name": "agent",
      "purpose": "Agent crate namespace for model/session/task/prompt/step/plan orchestration; lib.rs exposes the plan queue module, and task runtime uses PlanQueue/PlanRecord/PlanError for plan storage and complete-step gating."
    },
    {
      "path": "src/client",
      "name": "client",
      "purpose": "Client crate namespace for the marix_client library, ClientSession and signature-aware ClientEvent source, and client binaries including a CLI whose oneshot mode exits on Done completion events; ClientSession creates task signatures with empty names and leaves request text in TaskEvent::Create."
    },
    {
      "path": "src/common",
      "name": "common",
      "purpose": "Shared common crate namespace for config loading with serializable System/Platform/Arch re-exports, external adapters, logging, and structure utilities including WorkQueue::clear for session cleanup."
    },
    {
      "path": "src/prompt",
      "name": "prompt",
      "purpose": "Owns prompt templates used by model-facing steps, including no-Model Step kind formats, run_steps Plan output examples, answer-only Initial JSON for no-step requests, and Intent text in the surrounding step description."
    },
    {
      "path": "src/protocol",
      "name": "protocol",
      "purpose": "First-class marix-protocol workspace crate for reusable session, task, plan, step, execution, tool, and shared Signature trait definitions; Plan includes run_steps, pending_steps, serde_json parsing for completed model-step output, and PlanSignature implements Signature."
    },
    {
      "path": "src/host",
      "name": "host",
      "purpose": "Host crate namespace for HostSession and host-side tool execution; executor preview replies send System::new host metadata and tool previews, and unknown-tool failures send ExecutionStatus failure events."
    },
    {
      "path": "src/tool",
      "name": "tool",
      "purpose": "Top-level marix-tool crate that defines ToolProgram and native tool executables; it now depends on marix-protocol for tool preview/schema protocol types."
    }
  ],
  "elements": []
}
