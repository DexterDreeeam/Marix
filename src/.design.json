{
  "schemaVersion": 1,
  "module": {
    "path": "src",
    "name": "src",
    "purpose": "Rust workspace root for flat Marix package modules and session-pipe protocol support.",
    "changeStatus": "unchanged"
  },
  "childModules": [
    {
      "path": "src/common",
      "name": "common",
      "purpose": "Shared protocol, session pipe, and helper crate package."
    },
    {
      "path": "src/config",
      "name": "config",
      "purpose": "Configuration library crate package with flat source files."
    },
    {
      "path": "src/core",
      "name": "core",
      "purpose": "Core library and server binary crate package with workflow runtime, session server, and preprocess modules."
    },
    {
      "path": "src/cli",
      "name": "cli",
      "purpose": "CLI binary crate package with session pipe client and local integrated core mode."
    }
  ],
  "elements": []
}

