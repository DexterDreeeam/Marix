{
  "schemaVersion": 1,
  "module": {
    "path": "src",
    "name": "src",
    "purpose": "Rust workspace source root for Marix crates with a flat Cargo crate layout plus prompt-template sources. It organizes agent, client, common, prompt, host, protocol, and tool crates/modules; the client crate now emits signature-aware common events so CLI output can group contiguous task/step chunks by signature id.",
    "changeStatus": "modified"
  },
  "modified": [
    "."
  ],
  "childModules": [
    {
      "path": "src/agent",
      "name": "agent",
      "purpose": "Agent crate namespace for model/session/task/prompt orchestration; it carries host System metadata through SessionContext into initial prompt rendering and supports conditional prompt templates."
    },
    {
      "path": "src/client",
      "name": "client",
      "purpose": "Client crate namespace for the marix_client library, ClientSession and signature-aware ClientEvent source, and the marix-client-cli and marix-client-app binary modules."
    },
    {
      "path": "src/common",
      "name": "common",
      "purpose": "Shared common crate namespace for config loading with serializable System/Platform/Arch re-exports, external adapters, logging, and structure utilities including WorkQueue::clear for session cleanup."
    },
    {
      "path": "src/prompt",
      "name": "prompt",
      "purpose": "Owns prompt templates used by model-facing steps, including an Initial prompt that declares System separately from conditional previous-task Context rendering."
    },
    {
      "path": "src/protocol",
      "name": "protocol",
      "purpose": "First-class marix-protocol workspace crate for reusable session, task, step, execution, tool, and shared Signature trait definitions; task/execution/step signatures implement Signature, with step ids derived deterministically via uuid v5."
    },
    {
      "path": "src/host",
      "name": "host",
      "purpose": "Host crate namespace for HostSession and host-side tool execution; executor preview replies now include System::new host metadata and tool previews."
    },
    {
      "path": "src/tool",
      "name": "tool",
      "purpose": "Top-level marix-tool crate that defines ToolProgram and native tool executables; it now depends on marix-protocol for tool preview/schema protocol types."
    }
  ],
  "elements": []
}
