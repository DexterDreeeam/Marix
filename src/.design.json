{
  "schemaVersion": 1,
  "module": {
    "path": "src",
    "name": "src",
    "purpose": "Rust workspace root for flat Marix package modules and pipe sender/listener interfaces.",
    "changeStatus": "modified"
  },
  "modified": [
    "."
  ],
  "childModules": [
    {
      "path": "src/cli",
      "name": "cli",
      "purpose": "CLI binary crate package using pipe sender boundary."
    },
    {
      "path": "src/common",
      "name": "common",
      "purpose": "Shared protocol, session, and pipe interface crate package."
    },
    {
      "path": "src/config",
      "name": "config",
      "purpose": "Configuration library crate package with flat source files."
    },
    {
      "path": "src/core",
      "name": "core",
      "purpose": "Core pipe server binary crate package centered on common UserMessage pipe handling and the preprocessing boundary."
    }
  ],
  "elements": []
}
