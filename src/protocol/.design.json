{
  "schemaVersion": 1,
  "module": {
    "path": "src/protocol",
    "name": "protocol",
    "purpose": "Owns the marix-protocol crate definitions and namespace exports for reusable session, task, step, execution, and tool protocol contracts. TaskPreview now carries only request/result snapshots without an embedded TaskSignature; message.rs defines the SessionMessage network envelope.",
    "changeStatus": "modified"
  },
  "childModules": [
    {
      "path": "src/protocol/task",
      "name": "task",
      "purpose": "Owns task protocol contracts including task identifiers, events, request previews, signature-free TaskPreview request/result snapshots, signatures, results, and statuses."
    },
    {
      "path": "src/protocol/step",
      "name": "step",
      "purpose": "Owns step protocol contracts including executable drafts, planning output, kinds, signatures with descriptions, events, results, statuses, and previews."
    },
    {
      "path": "src/protocol/execution",
      "name": "execution",
      "purpose": "Owns execution protocol contracts including execution ids, events, requests, signatures, updates, and statuses."
    },
    {
      "path": "src/protocol/session",
      "name": "session",
      "purpose": "Owns the SessionEvent protocol enum."
    },
    {
      "path": "src/protocol/tool",
      "name": "tool",
      "purpose": "Owns tool schema and preview protocol contracts."
    },
    {
      "path": "src/protocol/external",
      "name": "external",
      "purpose": "Local third-party re-export wiring for protocol internals."
    }
  ],
  "elements": [
    {
      "name": "SessionMessage",
      "type": "struct",
      "source_depth": 2,
      "changeStatus": "unchanged",
      "codeSegments": [
        {
          "sourcePath": "src/protocol/message.rs",
          "lineStart": 7,
          "lineEnd": 11,
          "language": "rust",
          "addedLines": [],
          "modifiedLines": []
        },
        {
          "sourcePath": "src/protocol/message.rs",
          "lineStart": 13,
          "lineEnd": 25,
          "language": "rust",
          "addedLines": [],
          "modifiedLines": []
        }
      ]
    }
  ],
  "modified": [
    "."
  ]
}
