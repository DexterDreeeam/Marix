[package]
name = "marix-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
marix-common = { path = "../common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
uuid = { version = "1", features = ["serde", "v4", "v5"] }

[lib]
name = "marix_protocol"
path = "lib.rs"
