[package]
name = "marix"
version = "0.1.0"
edition = "2021"
description = "Composable CLI/core/config agent architecture."
license = "MIT"

[lib]
name = "marix"
path = "lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
