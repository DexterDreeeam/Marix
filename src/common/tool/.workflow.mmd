flowchart LR
  %% Shared tool metadata contracts; Tool method bodies are panic-backed placeholders.
  subgraph schema_rs[schema.rs]
    ToolSchema["ToolSchema\nstruct"]
  end

  subgraph tool_rs[tool.rs]
    ToolPreview["ToolPreview\nstruct"]
    Tool["Tool\nstruct"]
  end

  ExternalTool["external tool executable\n(host/tool binary)"]

  ToolPreview -->|"schema field"| ToolSchema
  Tool -->|"preview() returns"| ToolPreview
  Tool -->|"input_schema()/output_schema() shape"| ToolSchema
  Tool -->|"load(path) / execute(input)"| ExternalTool
