# engineer-of-source experience — Marix

- 2026-08-01: Relay background/current context headers now live in `src/server/prompt/module/BackgroundTaskContextHeader.prompt` and `CurrentTaskContextHeader.prompt`; `server/relay/prompt.rs` trims the module file ending before restoring exact LF-only separators (`\n\n\n` before the first ancestor, `\n\n` before the current Goal), preserving the former hardcoded model-visible bytes despite CRLF prompt files.
- 2026-08-01: Production benchmark prompt promotion maps `system`, `policy`, and `completed_notice` into the three prompt templates, while relay-owned `background_header` and `current_header` remain string prefixes in `src/server/relay/prompt.rs`; preserve the existing three-newline ancestor separator and pending-intent suffix logic.

## Workflow runtime

- The active hierarchy is Task → Intent → Step → Invocation/Relay. Intent owns plain Plan state; there is no active Plan actor or protocol namespace.
- Register every child actor in its task-wide registry before `start`. On termination, commit lifecycle result/status and complete the registry entry before notifying the owner; reversing either order creates lookup races.
- `Lifecycle` stores `ActorStatus<Result>` atomically. Parent updates carry the terminal child result; use registry result lookup only for dynamic reconstruction.
- Task uses a current-thread Tokio runtime driven by a dedicated standard thread. Spawn child actors through that shared runtime; blocking the task dispatcher prevents every nested actor from progressing.
- `TaskAccess` permits construction/insertion only. Keep lookup, completion, cancellation, lineage checks, and routing in `TaskRuntime`; retain its weak `SessionContext` link.
## Model and tool boundaries

- Deepseek is stream-only. Buffer SSE tool-call fragments by index, require a valid terminal `[DONE]`, validate complete JSON-object arguments, then emit one normalized `StepDraft`; never execute partial deltas.
- 2026-07-20: DeepSeek chat-completion payloads explicitly disable Thinking in the base payload because Thinking rejects `tool_choice`; keep forced tool relays on `tool_choice: "required"` and non-tool relays on JSON response format.
- Invocation validates the exact registered tool name and JSON arguments. Return validation and unknown-tool failures through normal typed results so parent actors cannot strand.
- Windows managed processes launch executables directly with explicit arguments. Validate UUID paths, reject symlinks, bind ownership to PID plus creation time, bound output pages, and retain the lock file.
- 2026-07-21: Native web search invokes DuckDuckGo, Yahoo, and Wikipedia through curl with a 60-second per-attempt `--max-time`; preserve provider fallback order and retry settings together.
- 2026-07-22: Tool-call-summarize is a native Workflow Tool call (`workflow_call_summary`, in `protocol/workflow/call_summary.rs`), not an isolated JSON-mode relay — `RelayKind::ToolCallSummarize` is a struct variant `{ invocation: InvocationSignature, tool: String, output: String }` (sibling to the unit variant `RelayKind::IntentAnalyze`), and `RelayRuntime::model_request`/`context_prompts` in `server/relay/prompt.rs` stay a *single unconditional* code path shared by both variants; only one extra trailing prompt string differs, appended via `if let RelayKind::ToolCallSummarize { tool, output, .. } = &self.kind { prompts.push(Self::tool_call_prompt(tool, output)); }` right after `pending_intent_prompt`. This keeps the system prompt + every context-chain prompt except the last byte-identical between a normal decision call and a summarize call for the same intent state — that prefix-identity (for model-provider prompt/token-cache reuse) is the entire point of the design, so any future change to this path must preserve it rather than re-forking per-`RelayKind` request builders.
- 2026-07-27: Per-chunk tool-output summarization used to be amnesiac: `InvocationRuntime` already accumulated each chunk's summary in its private `summaries: StdMutex<Vec<String>>` (pushed in `on_summarize_update`, joined in `finish_summary`), but that list was never sent outward, so every chunk after the first was summarized with zero knowledge of what earlier chunks found. The fix threads it through `RelayKind::ToolCallSummarize.previous_summaries: Vec<String>` (`#[serde(default)]`), snapshotted in `summarize_and_finish` via `Self::lock(&self.summaries).clone()` — at that point the list holds exactly the PREVIOUS chunks, since the current chunk is only pushed after its relay returns. Reuse that one field; do not add a parallel list.
- 2026-07-27: Conditional prompt content must be carried in a parameter value, not as static template text, whenever the no-content render has to stay byte-identical. `Prompt::prompt()` rejects only `None` injections, so `Some("")` renders as nothing, and `Prompt::slice_marker` emits each `{{#name}}` as its own slice — so `{{#pre_chunk}}[TOOL: {{#tool}}]` on one line collapses to exactly `[TOOL: web_fetch]` when empty. `RelayRuntime::render_prompt` requires every template placeholder to have a supplied value but silently ignores extra supplied parameters (it iterates `prompt.parameters()`, not the supplied slice), so over-supplying is safe while under-supplying is not.
- 2026-07-27: `ExecutorCache::try_cache` returns `Ok((content, None))` once the remainder is at or below `CACHE_THRESHOLD_BYTES`, so the FINAL chunk of a continuation chain arrives with no cursor and renders through `ToolCallSummarize.prompt`, not `ToolCallSummarizeWithCursor.prompt`. Any per-chunk context added for chunked output must go into BOTH templates or the last chunk stays blind.
- 2026-07-27: Static model-facing prose belongs in `prompt/module/*.prompt`, but only the *static* part: `RelayRuntime::pre_chunk_text` keeps its `[PRE-CHUNK: {tool}]` header and `{n}. {summary}` list in Rust (variable-length data the template engine cannot express — it has no loop/conditional syntax) and loads only the trailing fixed sentence via `Self::render_prompt(module, Prompt::load_module, &[])`. Module files are CRLF-terminated on disk here, so `trim_end()` the render before re-appending the exact `"\n\n"` the Rust code used, or a third newline leaks in. A module name may mirror the template pair convention (base name plus a `WithCursor` suffix, as in `ToolCallSummarize`/`ToolCallSummarizeWithCursor`), but `PreChunk` deliberately has no `WithCursor` twin any more — see the 2026-07-27 collapse entry below; `Prompt::assert_identifier` allows only ASCII alphanumerics and underscore, so hyphens are rejected.
- 2026-07-27: Moving a Rust literal into a `.prompt` file makes the containing helper fallible; keep any early "renders nothing" return above the load so the common path stays infallible and filesystem-free. That property is provable at runtime, not just by inspection: point `MARIX_CONFIG` at a nonexistent path and confirm the empty-input call returns `Ok("")` while a non-empty call returns `Err("failed to load prompt ...: failed to load config for prompt ...")`.
- 2026-07-27: `RelayRuntime::append_tool_calls` is called with two *opposite* meanings and now takes a private `ToolCallFraming` enum to keep them apart: `Background` (from `plan_prompt`, ancestor history that the `[BACKGROUND CONTEXT]` wrapper already declares "for reference only") and `Completed` (from `pending_intent_prompt`, the current Intent's own executed calls). Both used to emit `[**BACKGROUND**]:`, so the model read its own confirmed results as reference-only background and re-ran identical `web_fetch` calls up to 6 times before completing. `Completed` emits `[**COMPLETED CALLS**]\n` plus the static `prompt/module/CompletedCalls.prompt` sentence; the `\n{index}. {tool}{descriptor}:\n{output}` item loop and the `Background` heading are shared and unchanged. Any future third caller must pick a variant, never reuse `Background` for confirmed work product.
- 2026-07-27: Inside `server/relay/prompt.rs`, use `Self::render_prompt(name, Prompt::load_module, &[])` and do NOT add your own `std::panic::catch_unwind` — `render_prompt` already wraps the loader (`Prompt::load*` panics rather than returning `Result`) and converts the payload into `failed to load prompt {name}: {detail}`. `IntentRuntime::check_repeat_invocation` in `server/intent/runtime.rs` hand-rolls that same `catch_unwind` only because it lives outside `RelayRuntime` and cannot reach `render_prompt`; copying its shape into `prompt.rs` would double-wrap.
- 2026-07-27: To prove a *branching* prompt refactor left one branch byte-identical, paste the pre-change function body verbatim into a throwaway `#[cfg(test)] mod` in the same file as `legacy_*`, render both against one shared fixture, and `assert_eq!(new.as_bytes(), legacy.as_bytes())` — this beats eyeballing a diff because it also covers the shared tail (here the `call_descriptor`/`single_line` item loop). `Prompt::load_module` needs only a concrete `config.toml` in `%TEMP%` with `marix_path` pointing at the absolute `src/server` directory plus `$env:MARIX_CONFIG`; no need to mirror `.prompt` files into a scratch tree since the real ones already sit under `src/server/prompt/`. `IntentContext` fixtures are cheap: `TaskSignature::new`/`IntentSignature::new(task, None, name)` and plain struct literals for `StepResult`/`ToolCallResultDraft`/`InvocationResult`.
- 2026-07-27: `deployment/win/02-build-and-package.ps1::Copy-ServerPromptDirectory` copies `src\server\prompt` with `Get-ChildItem -File -Recurse -Filter '*.prompt'` and rebuilds each relative path under the output, then asserts source/destination counts match — so a new `prompt/module/*.prompt` ships automatically with no packaging edit, and any non-`.prompt` sidecar file placed there silently does not.
- 2026-07-27: To prove a prompt-relocation refactor is byte-identical, dump the pre-change helper output to files from a throwaway `#[cfg(test)] mod` inside the same source file (private associated fns are reachable from a child module), then re-run the same dump after the change and `assert_eq!` against the saved bytes. `Prompt::load_module` needs a real config: write a full `config.toml` into a scratch root with `marix_path = "."`, `unsafe { std::env::set_var("MARIX_CONFIG", ...) }`, and mirror the real `.prompt` files into `<root>/prompt/module/`. `Config::load` caches process-globally, so run each such test as its own `cargo test ... -- --exact` invocation. Rust lib-test filters need the full module path (`relay::prompt::scratch_prechunk::name`), not the bare module name.
- 2026-07-27: Multi-line Rust string literals using trailing `\` continuations collapse to a SINGLE line — the backslash eats the newline and the next line's leading whitespace — so `"a \`<newline>`     b"` is `"a b"`, not `"a\n b"`. When relocating such a literal into a text file, dump the real runtime value first rather than transcribing the source by eye.
- 2026-07-27: `cargo clippy` fingerprints are mtime-based, so restoring files with `Copy-Item` (which preserves the source LastWriteTime) can make cargo reuse a stale rlib and report a phantom `E0559 ... has no field` against source that clearly has the field. When doing a stash-free A/B baseline by copying files back and forth, reset `LastWriteTime` to now afterwards and confirm the run actually printed `Checking <crate>` lines before trusting the comparison.
- 2026-07-27: `cargo fmt --check` is NOT clean at baseline in `marix-server`/`marix-protocol` — the codebase deliberately hand-wraps near 70 columns while rustfmt targets 100, so dozens of pre-existing diffs exist. Do not run `cargo fmt` to "fix" a touched file; match the surrounding hand-wrapped style instead, or the formatter will rewrite unrelated code.
- 2026-07-27: Prompt templates under `src/server/prompt/template/` are CRLF in the worktree while every Rust-built prompt fragment (`pending_intent_prompt`, `plan_prompt`, `pre_chunk_text`) uses `\n`. Keep `\n` in Rust — hardcoding `\r\n` would be wrong on Linux deployments — and accept the mixed endings inside a rendered message.
- 2026-07-22: `IntentRuntime::verdict()` (`server/intent/runtime.rs`) and the analogous `InvocationRuntime::render_workflow_policy()` (`server/invocation/runtime.rs`) both hardcode the literal string `"WorkflowPolicy"` three times (in `Prompt::load(...)`, the panic-detail format, and two more error messages) rather than factoring it into a local `template` variable — unlike `RelayRuntime::system_prompt`'s style in `server/relay/prompt.rs`, which does use a local `let template = "System";`. When adding a third `Prompt::load` call site for the same template name, match whichever sibling call site is closer in shape/intent rather than assuming one style is universal across the codebase.
- 2026-07-21: A model backend can be fully self-contained (e.g. `src/server/model/glm/`): own its own `XConfig` struct inside `backend.rs` instead of `common/config`, take it directly in `new(config)`, and stay unreferenced except for the two mirrored lines in `model/mod.rs` (`pub mod x;` / `pub use x::XBackend;`). A `pub use`-exported but never-constructed backend does not trigger a dead-code warning under `cargo check`, so an intentionally-unwired sibling backend compiles clean without `#[allow(dead_code)]`. GLM's chat-completions API is OpenAI-compatible SSE, so its stream accumulator mirrors Deepseek's byte-for-byte (same five-tool-call cap, same `stop`/`tool_calls` finish_reason values, same `thinking: {"type":"disabled"}` payload field).
- 2026-07-21: Deepseek's and Glm's near-duplicate stream/request/payload logic was consolidated into `src/server/model/openai.rs` (`pub(super) struct OpenAiCore`, sibling-scoped so `deepseek`/`glm` submodules can use it but nothing outside `model` can). Each backend now holds `core: OpenAiCore` and delegates `ModelBackendImpl`/`Debug`. Provider-specific text (log/error strings) is threaded through a `provider: &'static str` field/param instead of being hardcoded per backend — this is the general pattern for extracting shared logic behind a `pub(super)` core type when two+ sibling modules are byte-for-byte identical except for one label.
- 2026-07-23: Every `src/tool/native/*` binary is a one-shot OS process per `tool_main.rs`: reads one JSON payload from stdin, prints one JSON result, exits — no persisted state, no task/session id passed in (confirmed via `src/host/executor/tool.rs`, which spawns each tool with bare stdin/stdout/stderr pipes only). Any "remember more than fits" feature (pagination, continuation) for these tools must therefore be stateless re-fetch/re-request-and-reslice on every call, matching the existing internal convention in `tool/native/process/output.rs` (`OutputSnapshot::from_path`: structured `offset`/`max_bytes` in, `next_offset`/`truncated` out, `offset > length` errors, `offset == length` is a valid empty read) — never a disk cache, and never a natural-language "call again with X" hint embedded in the content string.
- 2026-07-23: `web_fetch.rs` and `web_search.rs` (`src/tool/native/web/`) intentionally use two different error-helper styles that must each be preserved on their own file: `web_fetch.rs`'s `failure()` is a free module function (not `Self::`), so its own size-limit consts (`DEFAULT_MAX_LENGTH`/`MAX_MAX_LENGTH`) were added as free top-level `const`s (not inside `impl WebFetch`) specifically so error messages can use plain inline format captures like `{MAX_MAX_LENGTH}`; `web_search.rs`'s `failure()` is `Self::failure` and its `MAX_RESULTS` lives inside `impl WebSearch`, so its analogous message must use a non-inlined positional arg (`"...to {}", Self::MAX_RESULTS`) since `Self::X` path expressions cannot be inlined in a format string. Match whichever style a given file already uses rather than normalizing both to one convention.
- 2026-07-23: Wikipedia's `opensearch` API (`search_wikipedia` in `web_search.rs`) is prefix/relevance search, not count-based pagination — it has no `offset`/`s`/`b`-style parameter and always searches from the top regardless of caller-requested offset. When threading an `offset` through `WebSearch::search`'s provider fallback chain (DuckDuckGo → Yahoo → Wikipedia), only the first two providers gained the parameter; Wikipedia's call signature and behavior stayed untouched as a deliberate, documented limitation, not an oversight.
- 2026-07-23: Tool-output continuation is a Host platform concern, not native-tool pagination: `host/execution/runtime.rs` passes completed ordinary output through the process-global cache in `host/executor/output_cache.rs`, while `host/executor/runtime.rs` intercepts `workflow_continuation` before registry lookup and serves one cached page through normal Invocation events. Cache entries are FIFO (10), task-owned, and byte-offset cursors never reach Tool EXEs.
- 2026-07-23: `host/executor/cache.rs::ExecutorCache` supersedes the task-owned offset design above: it stores only content plus opaque UUID cursors, consumes an entry on every `pick`, and re-caches only the remainder after a UTF-8-safe 16 KiB chunk. `host/execution/runtime.rs` leaves short ordinary output uncached and locks the shared `ExecutorState` cache only after Tool execution; `host/executor/runtime.rs` uses the same cache for `workflow_continuation`.
- 2026-07-27: `ExecutorCache::generate_cursor`'s `tc_` cursor is a short-lived, in-memory-only opaque key compared solely by plain string equality in `remove_entry`, capped at `CACHE_CAPACITY = 10` entries — no other module or test in the workspace assumes any particular cursor length/format, so shortening it (32 hex chars → 8) was a pure `generate_cursor`-local change with zero ripple. `ToolCallSummarize[ByCursor].prompt` fence tool output in a Markdown triple-backtick block instead of a `Result:` label line (`RelayRuntime::tool_call_prompt` in `server/relay/prompt.rs` just substitutes `{{#tool}}`/`{{#output}}`/`{{#continuation_cursor}}` into the template text verbatim, so static wording/formatting inside the `.prompt` file is free to change without touching Rust); the cursor variant's own label became the bracketed `[CONTINUATION CURSOR] {{#continuation_cursor}}` tag to match the family's existing `[TOOL CALL]`/`[TOOL: ...]`/`[CURRENT TASK]` bracketed-tag convention.

## Rust language gotchas hit during refactors

- When a private helper takes two-or-more reference-shaped parameters (e.g. `provider: &str` plus `value: Option<&serde_json::Value>`) and its return type borrows from only one of them, elision fails with E0106 ("missing lifetime specifier") because 2+ input lifetimes make the borrow source ambiguous. Fix by naming an explicit lifetime tied only to the parameter actually borrowed from, e.g. `fn f<'a>(provider: &str, value: Option<&'a Value>) -> Option<&'a [Value]>` — leave the unrelated parameter's lifetime elided/independent.
- If a `#[derive(Clone)]` struct gains a field whose type does not itself derive `Clone`, the derive fails at the *call site* (`Backend: Clone` bound not satisfied), not at the field's own definition — when wrapping shared state (e.g. `OpenAiCore`) inside multiple `Clone`-deriving wrapper structs, add `#[derive(Clone)]` to the shared type itself. `reqwest::Client` is `Arc`-backed internally, so cloning it is cheap and safe to do per-request/per-task.
- Clippy's `too_many_arguments` (default threshold 7) can appear as a *new* warning purely from splitting one bundled config struct into N free function parameters during a refactor, even when total information carried is unchanged. Prefer cloning the owning struct (`self.clone()`) into a single owned local and passing that one value into the spawned task/helper, over decomposing it into many scalar parameters — this is usually also a more faithful read of "use `self.field`"-style refactor instructions.
- The Rust coding style doc's ~500-line soft-file-size guideline can be explicitly overridden by an explicit task instruction to consolidate into "a single file, not a subdirectory" — when the two conflict, the explicit, more specific instruction wins; note the override rather than silently splitting the file back up.
- 2026-07-21: On this workspace's cargo version, `cargo fmt --check --workspace` errors ("unexpected argument '--workspace'" — it wants workspace args after `--`); use `cargo fmt --all --check` instead to format-check every workspace member.
- 2026-07-22: `clippy::large_enum_variant` (default ~200-byte threshold) can newly trigger purely from reshaping a variant from a tuple with one `InvocationSignature` field into a struct variant adding two more `String` fields — before concluding a clippy warning is "newly introduced by this change" rather than pre-existing, do a controlled A/B: temporarily swap back to the old shape, re-run clippy scoped to just the affected crate, and confirm the warning disappears. This codebase already carries several pre-existing, unaddressed instances of the same lint elsewhere (e.g. `SessionEvent` in `protocol/session/event.rs`, `StepEvent` in `protocol/step/event.rs`) without boxing large fields, so treat `large_enum_variant` as an accepted/tolerated lint class here — don't silently "fix" it by boxing fields when the enum shape itself was explicitly spec'd verbatim by the task.
- 2026-07-22: After swapping a file's content out-of-band via PowerShell (`Set-Content`/`Move-Item`) for a throwaway A/B clippy test and then restoring the original bytes, `cargo check`/`clippy` can report a suspiciously-fast "Finished" (around 1s) without actually recompiling the crate — Cargo's fingerprint check can fail to observe a real mtime change across the round-trip swap, so it silently reuses a stale cached artifact reflecting the *intermediate* (wrong) content, and the *next* `cargo check`/`clippy` invocation then surfaces spurious compile errors against what is actually correct, restored source. Fix: after any out-of-band file-swap test, force `(Get-Item path).LastWriteTime = Get-Date` and/or `cargo clean -p <pkg>` before trusting the next `cargo check`/`clippy` run — a single suspiciously-instant "Finished" is not proof of a genuine clean recompile.
- 2026-07-21: A "discriminator + N mandatory payload sections" config shape (e.g. `selected: String` plus always-required `deepseek: DeepseekConfig, glm: GlmConfig`) is strictly simpler to deserialize than a data-carrying enum: every section is a plain non-`Option` field, so a missing section surfaces for free as serde's own "missing field" error (bucketed under the existing `ConfigError::Toml` variant) — no custom `ConfigError` variant or manual validation needed. When replacing a data-carrying enum with this shape, `RawXxxx` intermediate-deserialization structs typically become unnecessary too: derive `Deserialize` directly on the real public struct, and reserve `#[serde(default = "...")]`/`#[serde(skip, default = "...")]` only for the few fields that are genuinely optional-with-a-computed-default (e.g. `core: CoreConfig` via `#[serde(default = "default_core_config")]`, `system: System` via `#[serde(skip, default = "System::new")]`) or need post-`toml::from_str` path resolution (e.g. `runtime`, `tool.directory`) — `build_config` then collapses to one `toml::from_str` call plus a couple of targeted field mutations, not a full manual struct reconstruction.
- 2026-07-21: A `grep` pattern using a long `|`-alternation group inside a quoted-string literal match (e.g. `"name": "(Config|RuntimeConfig|...|CONFIG_FILE)"` with 10+ alternatives) can return zero matches even though the content is present and a simpler single-term grep for one of the same alternatives succeeds immediately after — root cause not fully diagnosed (possibly an alternation-count limit or literal-parenthesis/quote escaping interaction). Workaround: prefer short, simple patterns (2-4 alternatives, or one distinctive substring) over long enumerated alternations when grepping JSON/text metadata.
- 2026-07-21: When a chain of two uncommitted tasks does "design A landed, then a follow-up task fully undoes/replaces design A with design B" without an intervening commit, `git diff` against the last real commit shows only the *net* structural delta — any type/field that was added by the first task and removed by the second never appears in the diff at all (as if it never existed). This is a good sanity check for design-metadata status decisions: if a `design.json` element was marked `added` (not `modified`/`renamed`) because "no prior evidence of it existing," a clean `git diff` against HEAD showing that same identifier as a fresh `+` addition (with no corresponding `-` removal of a same-named-but-different-shaped prior version) corroborates the call.
- 2026-07-24: `cargo clippy --workspace --all-targets --all-features -- -D warnings` is NOT clean on this workspace's committed baseline (pre-existing debt, unrelated to any one change) — it fails on a dozen-ish distinct lint violations spread across `marix-common` (`module_inception`, `new_without_default`, `derivable_impls`, `while_let_on_iterator`, `type_complexity`, `too_many_arguments`, in files like `actor/mod.rs`, `actor/lifecycle.rs`, `config/sys.rs`, `logging/source.rs`, `logging/store/*.rs`, and `structure/channel.rs`'s own pre-existing `accept_loop`/`connect_attempt` `setup_tx` parameter type) and `marix-host` (`module_inception` in `execution/mod.rs`/`executor/mod.rs`, `new_without_default` for `ToolRegistry`). Plain `cargo clippy --workspace --all-targets --all-features` (no `-D warnings`) does exit 0 today and matches the coding-style doc's actual documented invocation — treat that as the real clean gate for this repo. To audit whether a specific change introduces *new* findings under the stricter flag, do a git-stash-based A/B: stash your own diff, rerun `-D warnings` on the pristine baseline, compare the error sets, then restore your stash — don't assume `-D warnings` itself must pass.
- 2026-07-27: This workspace's vendored `uuid` 1.23.4 (`uuid::fmt::{Simple,Hyphenated,Urn,Braced}`, reached via `marix_common::external::uuid`) implements `Display`/`LowerHex` by calling `f.write_str(self.encode_lower(...))` directly (see its `impl_fmt_traits!` macro), never `f.pad(...)` — so a width/precision format spec like `format!("{:.8}", uuid.simple())` silently ignores the `.8` and still prints the full 32-hex-char string (confirmed empirically: produced a 35-char `tc_`+32-hex result, not 8). This is general Rust behavior, not uuid-specific: any custom `Display` impl that skips `Formatter::pad` ignores precision/width/fill/align entirely. To truncate such a value, slice its `.to_string()` explicitly, e.g. `format!("tc_{}", &uuid::Uuid::new_v4().simple().to_string()[..8])` — verified this yields exactly `tc_` + 8 lowercase hex chars. When a task's suggested `{:.N}` snippet and an "if that doesn't work, use this slice instead" fallback are both given, empirically probe with a throwaway `#[test]` (delete it afterward) rather than assuming the first form compiles-and-truncates correctly.

## Transport and configuration

- Remoc setup must keep its connection future driven after returning channels. Abort guarded connection tasks on timeout/rejection; otherwise failed handshakes leak workers.
- Server channel listeners bind the IPv4 wildcard independently of `server.ip`; connectors resolve the configured address. Server-driven host registration sends session identity and tool query before publishing the channel.
- Install a remote telemetry sink only after TCP, remoc, and token acceptance succeed. Startup telemetry exhaustion is fatal rather than a silent local fallback.
- 2026-07-24: `connect_channel<T>(endpoint)` in `structure/channel.rs` is a 1-line wrapper (`connect_channel_with_timeout(endpoint, CONNECT_TIMEOUT)`) around the real logic, which now lives in `pub fn connect_channel_with_timeout<T>(endpoint, timeout: Duration)` — this is the general pattern for giving one caller (here, Host) a different timeout/no-retry variant while every existing caller (Client, `Logger::connect_sender`'s 5-attempt retry loop, ~10 sites in `structure/tests/channel.rs`) keeps calling the original 1-arg function with zero call-site changes and byte-identical behavior. `Logger` mirrors this with `connect(source, timeout)` beside the untouched, still-5-attempt-retrying `connect_with_retry(source)` (renamed 2026-07-24 from `connect_once`/`connect` respectively, to resolve the naming ambiguity, pure rename with zero behavior change): both funnel through the same generic private `remote_sink(fallback_path, connect_sender: impl FnOnce() -> Result<NetSender<LogMessage>, LoggingError>)` helper, so `connect` only needed to supply a different one-shot closure (`connect_channel_with_timeout(..., timeout).map(...)`) — `connect_with_retry()`'s own `connect_sender()` (`MAX_ATTEMPTS = 5`, `RETRY_BACKOFF = 200ms`) was not touched.
- 2026-07-24: A bare `panic!()` inside a `std::thread::spawn`-ed worker thread does NOT terminate the OS process if the main thread's own loop ignores join results — confirmed in `host/main.rs`, whose main thread runs `session.run(); loop { std::thread::park(); }` after `HostSession::run` spawns the actual connect/worker thread, so a panicking worker would die silently while main parks forever (process alive-but-dead from outside). This is why Host's connect-failure and lost-connection paths in `host/session.rs::spawn_worker` call `std::process::exit(1)` explicitly instead of panicking: `process::exit` unconditionally kills the whole process from any thread, while a plain `panic!` only unwinds the thread that called it (this workspace's bins don't set `panic = "abort"`). Any future "this failure must kill the whole Host process" requirement inside a spawned thread must use `std::process::exit`, never rely on propagating a panic.

## Repository invariants

- Keep public protocol re-exports synchronized between `protocol/lib.rs` and its mirrored `protocol/mod.rs`.
- 2026-07-27: Adding a namespace to `marix-common` is a three-file edit: `src/common/<name>/<file>.rs` (implementation), `src/common/<name>/mod.rs` (`pub mod <file>;` + `pub use <file>::<item>;`, matching `structure/mod.rs`), and `src/common/lib.rs` (alphabetical `pub mod <name>;` plus a `pub use <name>::<item>;` line in the alphabetical re-export block). Consumers then import through their existing `use marix_common::{ ... };` block; rustfmt sorts lowercase function names after type names inside the brace list.
- 2026-07-27: `src/common/json/helper.rs::canonical_json_value` is the shared recursive object-key sorter for JSON values (arrays keep their order). Its explicit `sort_unstable_by` is load-bearing: `src/common/Cargo.toml` enables `serde_json`'s `preserve_order`, and Cargo unifies features workspace-wide, so `serde_json::Map` is insertion-ordered everywhere and serialization never sorts keys. It rebuilds objects through `.collect()` (`Map: FromIterator<(String, Value)>`) so `Map` never has to be named — `src/common/external/serde_json.rs` does not re-export `Map`. Inside `marix-common` reach `serde_json` via `use crate::external::serde_json;`, never `::serde_json::`.
- 2026-07-27: `IntentRuntime::repeat_invocation_key` (`src/server/intent/runtime.rs`) stays server-side because it encodes server policy — it strips the server-injected `purpose` argument and formats the `"{name} - {argument}"` counting key — while only the generic canonicalization lives in `marix-common`.

## Logging and telemetry

- 2026-07-22: Logging severity is `LogLevel` ordered by declaration as `Debug < Info < Warning < Error`; telemetry queries treat `level` as a minimum and storage verifies `message.level >= query.level`.
- 2026-07-22: Telemetry redb schema v5 owns `telemetry_session_level_emit`, deletes the legacy exact-level index during transactional rebuild, and reads old primary JSON through `#[serde(alias = "tag")]` while all new serialization emits only `level`.
- 2026-07-22: `LogMessage.tags: Vec<String>` (`#[serde(default)]`, schema v6) is deduplicated in first-seen order via `LogMessage::dedup_tags` at construction time (`LogMessage::new`) — a single message's own tag list is NOT sorted. This is deliberately different from `Store::tags(session_id)`'s session-wide distinct-tag listing, which returns an alphabetically-sorted `Vec<String>` via `BTreeSet`. Don't conflate the two ordering conventions when touching either.
- 2026-07-22: Pattern for adding an opt-in parameter to heavily-called public methods without touching their signature (used for `Logger::log/warning/error/debug`, hundreds of existing call sites across the workspace): add sibling `_tagged` methods (`log_tagged`/`warning_tagged`/`error_tagged`/`debug_tagged`, each taking `tags: impl IntoIterator<Item = impl Into<String>>`) and thread the new data through the shared private impl only — `emit`/`telemetry` both grew a `tags: Vec<String>` parameter, with the original 4 public methods passing `Vec::new()` and the new 4 passing `Self::collect_tags(tags)`. Every existing call site keeps compiling untouched.
- 2026-07-22: New `SESSION_TAG_TABLE` redb table (`src/common/logging/store/schema.rs`) is keyed by `session_tag_key(session_id, tag) = session_key(session_id) ++ tag.as_bytes()`, one empty (`b""`) valued row per distinct (session, tag) pair, populated inside `index_message` (shared by `record_batch` and `rebuild_indexes`) via `for tag in &message.tags`. `Store::tags(session_id)` (new `store/tags.rs`) range-scans just that session's key prefix, strips the prefix to recover each tag string, and collects into a `BTreeSet<String>` for free dedup+sort — this needed a `SCHEMA_VERSION` bump (5→6) so `ensure_schema()`'s `required`-table check forces old stores to `rebuild_indexes()` and backfill it. By contrast, tag *filtering* needed neither a new table nor a schema bump: `Store::matches(message, query, keyword)` in `reader.rs` just grew one more `&&`-chained OR-check (`query.tags.is_empty() || query.tags.iter().any(|tag| message.tags.contains(tag))`) appended after the existing level/keyword checks — since every scan path (`index_range_records`, `trigram_index_records`, `session_incremental_records`) already funnels through this one shared predicate, the new filter applies everywhere from a single edit site.
- 2026-07-22: `Logger::log_tagged`/`error_tagged` call sites take their tag list as an array literal (`["Model Relay"]`); growing it to N tags is a pure literal edit (`["Model Relay", "Request"]`) with zero syntax adjustment needed — a `[&'static str; N]` array satisfies `tags: impl IntoIterator<Item = impl Into<String>>` for any N, confirmed in `src/server/model/openai.rs`'s 4 call sites (moving `[Request]`/`[Response]` out of the message-text bracket and into a second tag, while `[{task_id}]` stayed in the text since task IDs are unbounded-cardinality and unsuited to the low-cardinality Tags dropdown).
- 2026-07-22: After a text-only shortening edit to a `Logger::*_tagged` call's `format!(...)` message argument, `cargo fmt` can leave one call site inline but reflow a **textually near-identical sibling** call site (same arg shapes/arity, similar string lengths, well under the 100-char `max_width`) into 4-line block form instead — the split is not simply "does the single-line version exceed max_width" (verified by measuring the hypothetical single-line reconstruction at 90 chars, under 100, and even reproducing a minimal standalone `rustfmt` snippet with the same shape that stayed inline). Cause not fully isolated; don't hand-guess/pre-format call sites after this kind of edit — just run `cargo fmt -p <crate>` and trust `cargo fmt --check -p <crate>` (must exit clean) as the only ground truth.
- 2026-07-25: Telemetry Portal polling in `http/telemetry.js` is serialized by one `logRequest` plus `generation`; the message modal must not gate that request path. Modal navigation in `telemetry-message.js` is record-ID anchored through `modalPosition()`, so live summary merges preserve the open message and only need to notify the helper to recompute Up/Down availability against the latest filtered array.
- 2026-07-25: A telemetry incremental page can advance `latest_record_id` while returning no items because current level/keyword/tag filters excluded new records. Use that watermark advance to reload the selected session's distinct tags; `telemetry-dropdown.js` must abort and identity-check overlapping tag requests, while preserving `selectedTags` until the existing session-change reset path runs.
- 2026-07-25: The bottom-first Telemetry Portal keeps browser summaries sorted `(emit_ts, id)` ascending while the HTTP page/cursor protocol stays newest-first. Initial/reset loads anchor at the bottom, `before` loads trigger near the top and restore `scrollTop` by the measured `scrollHeight` delta, and incremental/session polling only re-anchors when the viewport remained near the bottom through the request.
- 2026-07-25: Store open-time retention runs from `ensure_schema()` after schema validation: formal sessions are ordered by metadata `earliest_emit_ts` plus encoded session key, Empty records by `(emit_ts, record_id)`. Actual deletion and every secondary-index/metadata rebuild share one redb write transaction; rebuild takes the prior `META_NEXT_RECORD_ID` as a floor so deleting the highest historical ID never permits reuse.

## Design-metadata (`src_meta/`) nuances

- 2026-07-21: `pub(super)` counts as "outward-facing" for `design-json-update` element-tracking purposes, same as `pub`/`pub(crate)`/`pub(in ...)` — a `pub(super) fn` gets its own tracked element even though it never appears in the crate's genuine public API surface.
- 2026-07-21: When a private function is renamed and the *old* name was never itself tracked as a design.json element (e.g. it only became `pub(super)`/tracked-worthy as part of the same change that renamed it), classify it as a net-new `added` element at its new name — not a `renamed` element — because there is no prior element identity to rename in place. Reserve `renamed`/rename-in-place strictly for identifiers that already had their own tracked element under the old name.
- 2026-07-21: `src/common/structure/tests/channel.rs` (any path under a `tests/` folder, at any depth) gets zero design.json treatment — it is not scanned for elements and its changes never appear in any folder's status arrays, confirmed via both the `changed`- and `full`-mode path rules.
- 2026-07-21: Files/folders directly under `src/common/structure/` are covered by two design.json layers at once: `src_meta/common/structure/design.json` (native depth) and, one level up, `src_meta/common/design.json` — the latter carries a mirrored copy of the same `structure`-owned elements one layer down (per the "elements = own layer + direct child modules one layer down" rule), so both files must be edited together and kept in sync whenever a `structure/*.rs` element set changes. `src_meta/design.json` (repo root) only needs `modified: ["."]` plus purpose/child-summary wording when the change is a pure descendant with no direct file under `src/` itself.
- 2026-07-22: A task prompt asked this agent to itself recompute/correct `codeSegments` line ranges in `src_meta/server/model/design.json` after a `cargo fmt` reflow shifted line numbers in `src/server/model/openai.rs` (framed as "prior turns did this same fix"), but per this agent's own scope this agent never edits anything under `src_meta/` — that boundary held even though the request was phrased as a settled precedent. Findings were reported instead of applied: `OpenAiCore`'s 3rd codeSegment (`lineStart:71, lineEnd:168`, `modifiedLines:[{166,166}]`) needs its end and the modified-line span extended by the net insertion (log_response's call went from 1 line to 4, so `168→171` and `{166,166}→{166,169}`), and the 4th segment (`lineStart:170, lineEnd:242`, all `addedLines`) needs both bounds shifted +3 (`170→173`, `242→245`) — mirrored identically in `src_meta/server/design.json` (same element, byte-for-byte duplicate block). Also noticed pre-existing, unrelated-to-this-edit bad data on this same element: a 5th codeSegment claims `lineStart:700, lineEnd:709` in `openai.rs`, but the file only has 673 lines total (670 before this session's edit) — that range was already out-of-bounds before today's change and is not something this agent introduced or corrected.
- 2026-07-21: The "own layer + direct child module one layer down" mirroring rule is applied inconsistently pre-existing across the repo — confirmed by directly comparing three real files during one `config.rs` rewrite: `src_meta/common/config/design.json` (own-layer, depth-3) tracks public structs/enums plus `CONFIG_FILE` but *not* private free functions or the other private statics (`CONFIG_ENV_VAR`, `CONFIG_CACHE`); `src_meta/common/design.json` (mirror one level up) tracks the *same* structs/enums/`CONFIG_FILE` **plus** every private free function and both other private statics; `src_meta/server/design.json` (one level above `src/server/relay`) mirrors **nothing** from `relay`'s elements at all — just a `childModules` purpose-summary entry, no element list. When a change touches a deeply-nested file, always check the immediate-parent design.json's *existing* convention for that specific child (does it mirror elements at all? functions too, or just structs/enums?) rather than assuming one of the three patterns — do not try to harmonize them, per the skill's own explicit warning that conventions are inconsistent.
- 2026-07-21: An embedded test-fixture TOML string that lives inside a `#[cfg(test)] mod tests` block *within* an ordinary source file (e.g. `src/server_telemetry/server.rs`'s fixture, not a separate `tests/`-folder file) is not literally covered by the skill's "ignore folders named tests" rule, since it's not a folder — but the established repo precedent (confirmed by checking that an equivalent prior-task edit to the same fixture left that file's design.json completely untouched, `changeStatus: "unchanged"`, no top-level `modified` array at all) is to treat it the same way: skip design.json edits entirely for changes confined to an in-file `#[cfg(test)]` module, since test code isn't itself a tracked source element and the containing function's own `codeSegments` range doesn't extend into the test module.
- 2026-07-21: Promoting a dependency already resolved transitively (e.g. `socket2` locked at `0.6.4` via `tokio`) to a direct dependency at a compatible version constraint produces a minimal `Cargo.lock` diff — just one line added to the crate's own dependency list, no version/checksum churn elsewhere. Not a sign something is wrong; expected and cheap to verify with `git diff -- src/Cargo.lock`.
- 2026-07-21: `cargo fmt` will reflow a hand-written multi-line `.map_err(|e| ...)` closure or a multi-arg logging call into whatever line count it prefers (sometimes collapsing, sometimes expanding) — always run `cargo fmt -p <crate>` and re-`--check` after hand-editing non-trivial expressions from task prose rather than trusting the pasted formatting verbatim.
- Logging from deep inside `src/common` submodules (e.g. `structure/channel.rs`) uses `use crate::Logger;` (re-exported at the crate root), the same convention already used by `src/common/actor/actor.rs` and `runtime.rs` — check crate-root re-exports before assuming a fully-qualified module path is needed.
- 2026-07-22: A struct can be declared in a parent file while most of its `impl` blocks live in sibling files pulled in via `#[path=...]` mod declarations — concretely `Store` is declared in `src/common/logging/store.rs` (which also declares `mod schema; mod reader; mod tags; mod writer;`), but its methods live across `store/schema.rs`, `store/reader.rs`, and `store/tags.rs`. The design.json element is tracked at the *declaring* file's module layer (`logging/design.json`, not `store/design.json`), with one `codeSegments` array spanning all those sibling files. Check where the `struct`/`enum` keyword itself lives, never where most of the behavior lives.
- 2026-07-22: Confirmed concretely (via grep across all 51 `design.json` files in the repo) that the "own layer + direct child module one layer down" mirroring rule hard-stops at 2 layers with no exceptions: a `src/server/model/openai.rs` change (`OpenAiCore`) propagates to `server/model/design.json` and `server/design.json` only — it never appears in root `src_meta/design.json`, no matter how many ancestor folders exist above `model/`. Never propagate a deeply-nested file's element changes past its own folder plus that folder's immediate parent.
- 2026-07-22: A "pure text-shortening inside an existing `format!()` string, zero lines added/removed" edit is NOT guaranteed to leave line counts unchanged once `cargo fmt --check` is satisfied: shortening `Logger::error_tagged(format!("[Model Relay][{task_id}][Request] {error}"), ["Model Relay"])` from 4 lines down to under rustfmt's width limit made `cargo fmt` collapse it to 1 line — across 4 call sites in one file (`src/server/model/openai.rs`) this removed 12 lines total and shifted every element/impl-block boundary after the first edit site. Always re-derive exact current line numbers (grep for the element's opening/closing braces plus the specific changed lines) from the *post-`cargo fmt`* source, never from the pre-edit view, before touching `codeSegments` ranges in the module's own `design.json` and its one-layer-up mirror — both must be recomputed together (confirmed via the `design-json-update` skill, mode `changed`, driven by a `.temp/changed/<turn>.txt` marker) since both carry identical copies of the same element's segments.
- 2026-07-22: A private helper used by exactly one public function can be tracked as a second `codeSegments` entry bundled onto that public function's element rather than getting its own element — existing precedent: `parse_level` (private, `server_telemetry/handlers.rs`) is bundled onto `logs`'s element as its second segment. Preserve this bundling precedent where it already exists rather than splitting it into a separate element.
- 2026-07-22: Without git access, reconstruct pre-edit line-shapes via arithmetic: if N call sites are each claimed to transform the same mechanical way (e.g. "N single-line `Logger::log(...)` calls become N 4-line `log_tagged(...)` calls"), check whether the containing block's total observed line growth equals `N × (new-lines-per-call − old-lines-per-call)` for that shape — an exact match strongly corroborates the added/modified/pure-shift split without ever diffing against a prior commit.
- 2026-07-22: Confirmed concretely (via grep for the exact test fn names across all of `src_meta/`) that deleting two `#[test] fn` blocks from `message.rs`'s `mod tests` needed zero element-status changes — neither test name appeared anywhere in `design.json`, corroborating the line-57 rule that in-file `#[cfg(test)]` modules are never tracked. But it still required routine line-range housekeeping in both `src_meta/common/logging/design.json` and its mirror `src_meta/common/design.json`: `LogMessage`'s third `codeSegments` entry (the private `impl` block holding `dedup_tags`/`now_ms`, physically *after* `mod tests` in the file) had to shift its `lineStart`/`lineEnd`/`addedLines` upward by exactly the 20 removed lines (102→82, 117→97, 103-109→83-89) in both files identically, since that segment's bounds are absolute source line numbers and the deletion was upstream of it. General rule: after removing lines from a tracked file, always recheck every sibling `codeSegments` entry for that same file whose range starts *after* the deleted region, in every `design.json` that mirrors it — not just the ones for elements that "look related."
- 2026-07-22: **Validation-checklist blind spot**: the skill's own Validation steps catch only *structural* problems (JSON parses, required fields present, ranges in-bounds) — they cannot catch a codeSegment that's simply missing `modifiedLines` for a real change. Concretely, `Logger`'s public-impl segment initially had correct `addedLines` for 4 brand-new `_tagged` methods but an empty `modifiedLines`, silently missing that the 4 pre-existing `log/warning/error/debug` methods each had their `emit(...)` call site's argument list changed (one new trailing `Vec::new()` arg per call). This under-highlighting shape — a big new contiguous block correctly caught, several small scattered one-line pre-existing-call-site edits nearby missed — reproduces easily whenever N old call sites each gain "one more argument." Always re-read the live source line-by-line against recorded highlights for such elements before calling a design.json update done; the structural validation script alone is not sufficient evidence of correctness.
- 2026-07-22: The line-66 shift rule generalizes down to the smallest possible deletion: removing a single `assert!(message.tags.is_empty());` line from inside `message.rs`'s `mod tests` (one line, not a whole `#[test] fn`) again needed zero element-status change (test body isn't tracked) but still required the exact same downstream housekeeping — `LogMessage`'s third `codeSegments` entry (the private `impl` block after `mod tests`) shifted by exactly 1 line in both `src_meta/common/logging/design.json` and its mirror: 82→81, 97→96, and the nested `dedup_tags` `addedLines` highlight 83-89→82-88. Confirms the rule is deletion-size-agnostic: any N-line removal upstream of a segment shifts that segment's bounds by exactly N, down to N=1, with no lower bound where the housekeeping becomes optional.
- 2026-07-22: Deleting an entire `#[cfg(test)] mod tests { ... }` block outright (whole module, not just individual fns/lines inside it, per an explicit user instruction to remove test coverage rather than relocate it — done for `src/client/cli/main.rs`, `src/server_telemetry/server.rs`, and `src/common/logging/message.rs` in one turn) confirms both halves of the line-57/66/68 rule family at once: (1) `client/cli/main.rs`'s and `server_telemetry/server.rs`'s test modules were each the *last* thing in the file (EOF right after the closing `}`), and in both cases the pre-existing tracked element's own `codeSegments` (the `marix-client-cli` bin in `src_meta/client/cli/design.json` + its mirror `src_meta/client/design.json`; the `serve` fn in `src_meta/server_telemetry/design.json` + its mirror root `src_meta/design.json`) already ended exactly at the last production line *before* the test module (`main.rs` lineEnd 270, `server.rs` lineEnd 50) — confirming whoever tracked them originally already excluded the test module from the range, so a whole-module EOF deletion needs **zero** codeSegments edits in either the folder's own design.json or its one-layer-up mirror. (2) `message.rs`'s test module sat *mid-file* (private `impl LogMessage` for `dedup_tags`/`now_ms` follows it, separated by a `// -- Private -- //` marker), so deleting all 25 lines from the blank separator through the module's closing `}` (`#[cfg(test)]` at old line 54 through `}` at old line 77, plus the redundant leading blank line 53) required the now-familiar shift, this time by -25: `LogMessage`'s third `codeSegments` entry moved 81→56 / 96→71, `addedLines` 82-88→57-63, identically in both `src_meta/common/logging/design.json` and `src_meta/common/design.json`. When collapsing a leading blank-line-plus-module deletion into one `edit`/line-removal, keep exactly one blank line as the surviving separator (delete the *leading* blank, keep the *trailing* one already present before the next item) so `cargo fmt --check` doesn't trip on a doubled or EOF-trailing blank line — verified clean afterward with `cargo fmt --all --check` (note: `cargo fmt --check --workspace` is not valid syntax for `cargo-fmt`; `--workspace` is a `cargo` build-graph flag the `fmt` subcommand doesn't accept — use `cargo fmt --all --check` instead, `cargo fmt` is inherently workspace/all-local-packages-scoped when `--all` is passed).
- 2026-07-24: `Prompt::load()` (`src/server/prompt/prompt.rs`) resolves its template directory as `Path::new(&config.runtime.marix_path).join("prompt")` — flat, directly under `marix_path` — not `.join("src").join("server").join("prompt").join("template")`. This is a deliberate mismatch with the repo layout: the actual `.prompt` files still physically live at `src/server/prompt/template/*.prompt` (untouched by this change); the flat path targets a deployed/packaged bundle where a separate deployment-only script places `.prompt` files at `<bundle_root>/prompt/*.prompt` beside the bundle's own `config.toml`. Confirmed by repo-wide grep this is the only call site in `src/` constructing this path — no other file references the old nested pattern.
- 2026-07-25: Historical execution results for an intent are rendered by `RelayRuntime::append_tool_calls` in `src/server/relay/prompt.rs`; their section heading is model-significant context. `workflow_call_summary` output constraints are jointly owned by the ordinary/cursor prompt templates and the `summary` schema description in `src/protocol/workflow/call_summary.rs`, while cursor continuation policy remains isolated to the cursor template and `continuation_cursor` schema.
- 2026-07-25: `RelayRuntime::context_prompts` appends `WorkflowFocus.prompt` only for non-`ToolCallSummarize` relays; summarize relays retain `ToolCallSummarize[ByCursor]` as the final message. Packaging copies every file under `src/server/prompt/template` recursively, so new templates need no explicit deployment list.
- 2026-07-25: `src/tool/native/web/web_search.rs` preserves DuckDuckGo → Yahoo → Wikipedia fallback but invokes each provider only once per tool call: every provider launches one curl process without `--retry` or `--retry-delay`; the remaining loops only parse returned HTML. This supersedes the 2026-07-21 retry-settings clause—preserve provider order and the 60-second timeout, not same-provider retries.
- 2026-07-25: Web Search now launches Bing RSS → DuckDuckGo → Yahoo → Wikipedia concurrently with one `curl.exe` child per scoped worker. The shared request boundary forces IPv4, uses 4-second connect/12-second total timeouts, caps redirects/body size, and accepts a source only after final HTTP status and Content-Type metadata validation; aggregation round-robins successful sources in priority order and deduplicates by a normalized URL key.
- 2026-07-25: Bing Web Search uses the RSS 2.0 endpoint (`format=rss`) and `roxmltree` 0.20 in the `marix-tool` crate. `roxmltree` owns XML entity/CDATA handling; Web Search only strips description HTML and decodes residual common HTML entities after XML parsing.
- 2026-07-25: Host↔Server Core lifecycle telemetry is owned by `host/session.rs` and `server/session/runtime.rs` under the low-cardinality `Host Connection` tag. Match `NetReceiver::recv()` explicitly (`Ok(Some(_))` message, `Ok(None)` clean remote close/EOF, `Err(_)` receiver failure) and return one private termination detail carrying elapsed duration plus the successful receive count, so each side emits exactly one structured termination event. Host remains single-shot and exits the process with code 1 after its worker ends; Server logs cleanup only after clearing the existing Host/Client/context state, then relistens.
- 2026-07-25: `RelayRuntime::append_tool_calls` in `server/relay/prompt.rs` renders both current and ancestor ContextChain history. Background records are one line, and descriptors prefer non-`purpose` JSON fields only while their fully rendered Unicode length is under 32 characters; `workflow_*` history is purpose-only.
- 2026-07-25: ContextChain ancestors in `server/relay/prompt.rs` render `[Goal]` plus `[Plan]`; the first result-less subintent supplies the plan `Goal:` and `[EXECUTING]` marker, completed items emit trimmed output without status labels, and every historical tool descriptor is followed by its folded summary on the next line.
- 2026-07-25: The Telemetry Portal format-message modal is a flex child of a fixed, padded backdrop. With the page-wide `* { box-sizing: border-box; }`, `width: 100%` on `#format-message-modal` fills exactly the backdrop's padded content box without horizontal overflow; backdrop padding alone owns desktop/mobile viewport margins.
- 2026-07-25: `RelayRuntime::call_descriptor` chooses a non-empty ordinary-tool core descriptor when `core.chars().count() < purpose.chars().count() || core.chars().count() < 32` (both comparisons strict), while `workflow_*` remains purpose-only. Tool-call summaries inject output unchanged under `[TOOL: name]`; non-summary relays load the newline-free `WorkflowIgnoreToolCall.prompt`.
- 2026-07-25: Telemetry Portal's `#log-area` is the top-origin vertical flex scroll owner; `#log-table:not([hidden])` uses `margin-top: auto`, `flex-shrink: 0`, and bounded width so short virtual tables consume free space above and sit at the bottom, while overflowing tables have zero auto margin and preserve normal `scrollTop`/`scrollHeight` behavior for top-page loading and spacer virtualization.
- 2026-07-25: Info telemetry is named `Logger::info`/`info_tagged` in `common/logging/logger.rs` and `TaskLogger::info`/`info_tagged` in `protocol/task/logger.rs`; the task-scoped wrapper forwards its `TaskId` through `Logger::emit_for_task`. Migration scope includes active sources and hidden legacy `server/.relay`/`server/.task` sources, while `logging_tool::ToolLogger::log` and private connection-termination `log` helpers are unrelated APIs and must remain unchanged.
- 2026-07-25: `TaskLogging` is the task-owner convenience boundary in `protocol/task/logger.rs`: implementors supply a `TaskLogger`, while `info`/`warning`/`error`/`debug`/`info_tagged` delegate unchanged. Active Server task runtimes and Host `ExecutionRuntime` use it; request-local loggers in Server session, Host executor, and model helpers remain explicit because those owners do not hold one fixed task.
- 2026-07-25: Telemetry Portal's `#session-list` safely mirrors short-log bottom alignment by using an overflowed column flex list, non-shrinking `li` items, and `margin-top: auto` on the first item; unlike `justify-content: flex-end`, the auto margin yields to overflow so old Sessions remain reachable and existing `scrollTop`/`scrollHeight` anchoring stays valid.
- 2026-07-25: Telemetry Portal's Session panel has no heading; `#session-list` alone owns its reveal, mobile, and reduced-motion selectors, while `#session-content` and the first item's auto margin preserve full-panel sizing and bottom-up growth.
- 2026-07-25: `taskRowColors` in `server_telemetry/http/telemetry.js` preserves FNV-1a string hashing: null/undefined stay black, while every other task ID selects one integer from each disjoint low (0–12), mid (24–48), and high (64–96) band and one of all six RGB permutations, yielding stable dark colors without HSL or gray convergence.
- 2026-07-25: Active relay requests render one separate final system regulation: `ToolCallSummarize` uses `WorkflowToolAllowRegulation` for only `workflow_call_summary`, other relay kinds use `WorkflowToolRejectRegulation`, and `OpenAiCore::build_payload` appends that regulation after every user context prompt.
- 2026-07-25: `workflow_call_summary` declares `summary` before `continuation_cursor` in its raw schema; enabling serde_json's `preserve_order` feature in `marix-common` keeps that insertion order when the schema is parsed into `Value`, with workspace feature unification applying it to Server payload serialization.
- 2026-07-25: `taskRowColors` in `server_telemetry/http/telemetry.js` now derives disjoint low (0–8), mid (16–32), and high (40–64) channels from the existing FNV-1a hash before selecting one of six RGB permutations; this keeps every channel at or below 64 and guarantees one channel is at least 40.
- 2026-07-25: Relay context sections in `server/relay/prompt.rs` use `[**GOAL**]:`, `[**PLAN**]:`, and `[**BACKGROUND**]:`; the active plan item is `[EXECUTING NOW]`. Plan failures render ordered goal bullets under one `[**FAIL PLANS**]` heading, followed by each group's `(Failed Reason)`.
- 2026-07-26: `server/prompt/profile.rs::PromptProfile` is the single active owner of OpenAI-compatible message role/order composition and `tool_choice`: `ToolMandatory` requires any tool, while `ToolCallSummary` forces `WorkflowCallSummary::NAME`. Relay profile selection also chooses Allow/Reject regulation, but both profiles retain the same unfiltered, same-order tool list.
- 2026-07-26: `RelayRuntime::finish_succeed` parses the normalized `StepDraft` before completing an `IntentAnalyze` relay and rejects any `workflow_call_summary` call with a profile-specific failure, preventing it from reaching Intent workflow dispatch; summary relays retain their exactly-one-call and continuation-cursor validation.
- 2026-07-26: Superseding the two notes above, `ToolCallSummary` is now a Server-internal JSON type in `server/prompt/profile.rs`, not a Workflow Tool. Both relay profiles keep the same merged tool list; Summary uses `tool_choice: "none"`, JSON response format, content streaming, and a final summarize user prompt. Relay parses direct JSON and validates an optional cursor before Invocation requests `WorkflowContinuation`.
- 2026-07-26: Prompt modules live under `src/server/prompt/module` in source and resolve at runtime from `<marix_path>/prompt/module`; `Prompt::load` and `Prompt::load_module` share identifier validation, config/path/read checks, unsupported `[[#` rejection, and parameter slicing through one private loader. Relay module rendering shares the ordinary prompt panic capture, parameter injection, unsupported-parameter validation, and render-error path.
- 2026-07-27: Telemetry schema v8 keeps `telemetry_log_keys` as `u64 => &[u8]` but stores canonical 1–39 digit decimal ASCII strings derived from UUID `as_u128`; only the schema-v7 rebuild accepts non-decimal raw 16-byte UUID values and rewrites them transactionally. Current reads, writes, and maintenance reject non-canonical or overflowing values, and the HTTP API exposes keys as strings to avoid JavaScript precision loss.
- 2026-07-27: `PreChunkWithCursor.prompt` was deleted and `pre_chunk_text` lost its `continuation_cursor: Option<&str>` parameter (it selected nothing else), so one `PreChunk` module now serves both call paths. Rationale: continuation-cursor decision policy already lives in `ToolCallSummarizeWithCursor.prompt` ("If unseen later chunks may affect the [CURRENT TASK] ... also return `continuation_cursor` ..."), and duplicating it in a module made the no-cursor variant assert `and no further chunks will follow`, which is false for the FINAL chunk of a chain (see the `ExecutorCache::try_cache` entry above). Keep per-chunk module prose variant-neutral: state only what the accumulated facts are, never what to do about the cursor. `tool_call_prompt` still needs `continuation_cursor` for template selection and for pushing the `continuation_cursor` parameter -- do not remove it there.
- 2026-07-27: RESOLVED by user directive -- `ToolCallSummarize.prompt` now ends with `There are no further chunks, so do not return `continuation_cursor`.`, replacing the false `The output was not chunked.` claim. The no-cursor template serves BOTH a genuinely unchunked single-shot output and the FINAL chunk of a chain (`ExecutorCache::try_cache` returns `Ok((content, None))` once the remainder fits `CACHE_THRESHOLD_BYTES`), so the justification must be "no further chunks", never "never chunked". Keep the instruction to omit the cursor; do not reintroduce chunking claims that only hold for the single-shot case.
- 2026-07-27: A scratch render-proof test can exercise BOTH `tool_call_prompt` variants without constructing a `RelayRuntime`: that method never reads `self`, so a child `#[cfg(test)] mod` can replicate its body against `RelayRuntime::pre_chunk_text` / `RelayRuntime::render_prompt` (both private associated fns, visible to descendant modules) and mirror the real `.prompt` files into a scratch `<root>/prompt/{module,template}/`. Doing both variants in ONE `#[test]` sidesteps the process-global `Config::load` cache entirely -- no per-test `-- --exact` invocation needed.
- 2026-07-27: For a stash-free clippy A/B, cargo replays cached diagnostics for fresh units, so a full `cargo clippy --workspace --all-targets --all-features` after an edit still prints the whole workspace diagnostic set and is directly comparable to a pre-edit capture. Extract `^(warning|error)` headers paired with their following `-->` location and `Compare-Object` the sorted sets; a bare count is not enough. Confirm real `Checking <crate>` lines appeared for the edited crate first.
- 2026-07-27: Telemetry Portal dropdowns had TWO independent "closes/breaks while logs stream" causes, both proven with headless Chrome against the real assets. (1) `createDropdown`'s `window.addEventListener("scroll", close, true)` is CAPTURE phase, so it observes scrolls of ANY element even though scroll does not bubble; `requestLogs("incremental")` -> `scrollToBottom(logAreaEl)` fires a `#log-area` scroll every 2s and closed BOTH Level and Tags. Fix: close only when `_event.target.contains(_button)` (page/document scrolls still close; `#log-area` and the popup's own overflow list no longer do). (2) `renderTagsPopup` unconditionally `replaceChildren`-ed the Tags popup on every refresh, destroying the node under the pointer/focus. Fix: split into guarded `renderTagsPopup` (defers via `_tagsRenderPending` while `isOpen()`, still updates the button label) and unguarded `renderTagsPopupContent`; `createDropdown` gained an optional `_onOpen` invoked after `_open = true` but BEFORE `position()`, so the deferred rebuild is measured with fresh content. The open-time callback must call the UNGUARDED function -- calling the guarded one deadlocks (`_open` is already true, so it re-defers forever and the new content never appears). `toggleTag` must not rebuild either; it now patches only the clicked `<li>`'s `selected` class / `aria-selected` plus the button label.
- 2026-07-27: `src/server_telemetry/http` has no build step, so verify it by serving the real files from a scratch Node stub that mirrors `router.rs` route names (`/` -> page.html, `/telemetry.css` -> page.css, `/telemetry-*.js`, `/api/sessions`, `/api/sessions/{id}/tags`, `/api/logs`) and driving it with puppeteer installed OUTSIDE the repo under `%TEMP%`. `/api/logs` must return fresh items on every `after_id` poll to reach the `loadAvailableTags` branch at the end of `requestLogs`. Prove node identity with a JS expando (`li.__probe`) -- it survives in-place attribute edits and dies on `replaceChildren` -- and prove causality with a capture-phase scroll tracer plus MutationObservers on the popup's `hidden` attribute and `childList`.

- 2026-07-27: Telemetry Portal log rows dropped the right-click context menu: left click -> `openMessage(_summary, _row)` (format modal), right click -> `copyMessage(_event, _summary)` (`preventDefault` + clipboard + toast). Chrome fires NO `click` event for the secondary button (only `contextmenu`/`auxclick`), proven in headless Chrome with a capture-phase `click` recorder that stayed empty across a real `button:'right'` press -- so an `_event.button === 0` guard in the click handler would be dead code. Likewise, `inert` on `#app` plus the `position:fixed; inset:0` backdrop already make row clicks impossible while the modal is open (real mouse click at row coordinates produced one `click` whose target was the backdrop DIV, never a `#log-body` row), so `openMessage` needs no `_modalOpen` re-entry guard; the removed `openContextMenu` guard was not load-bearing. `_modalPreviousFocus` semantics are unchanged by moving capture to click time: `mousedown` on a non-focusable `<tr>` blurs any focused input first, so `document.activeElement` is `<body>` in both the old contextmenu handler and the new click handler. Deleting the menu also retired `bindAction` (its only two callers), the `pointerdown` outside-click handler, and the `scroll`(capture)/`resize` dismissal handlers; note `telemetry-dropdown.js`'s header comment still cites `openContextMenu/closeContextMenu` as its model and is now stale.

- 2026-07-27: Telemetry Portal log rows got their click affordance: `cursor: pointer` on `tbody tr.log-row` in `page.css`, plus a real hover lift. The `tbody tr.log-row:hover, tbody tr.log-row:focus-within { background: var(--task-background-hover); }` rule and the `--task-background-hover` custom property had existed all along, but `taskRowColors` returned `hoverColor === color` on BOTH branches (hashed and the null/no-task `#000` fallback), so the rule swapped a colour for itself and hovering repainted nothing. Proven in headless Chrome before the fix: `row.matches(":hover")` was `true` while `getComputedStyle(row).backgroundColor` per-channel delta was `[0,0,0]` on all four sampled rows -- the `:hover` state engaged, the value was just identical, so any test asserting only "the hover rule exists" or ":hover matched" would have passed against the bug. Assert the rendered `backgroundColor` differs across a real `page.mouse.move`. Fix is a flat additive `c_rowHoverLift = 12` applied uniformly to all three channels through `liftChannel`/`hoverColorFrom(_channels)`, which the null branch also calls as `hoverColorFrom([0, 0, 0])`; a uniform additive lift preserves each task's hue relationship, whereas a multiplicative one would leave near-zero channels dead. Base channels are tiny (`_hash % 4`, `8 + (%7)`, `16 + (%9)`), so 12 is clearly visible yet is only one third of the existing `#session-list li:hover` grey (`#242424` = 36) -- faint, not a selection bar. To keep the base palette byte-identical while reusing the permuted channels for the lift, hoist them into `var _ordered = [...]` first and build `_color` from `_ordered` with the same `", "` separators. No transitions: every existing hover in `page.css` (`#session-list li:hover`, `.dropdown-popup li:hover`, toolbar border-colour hovers) repaints instantly.

- 2026-07-27: `:focus-within` parity on a `<tr>` with no focusable descendants is testable without touching production markup: set `row.tabIndex = 0`, `row.focus()`, read `getComputedStyle(row).backgroundColor`, then `blur()` and `removeAttribute("tabindex")`. `:focus-within` matches the element itself, so this exercises the real CSS rule. Move the mouse away first and assert `row.matches(":hover") === false`, otherwise hover is what you are measuring.

- 2026-07-27: Telemetry schema is now v9 and log keys are FIXED-WIDTH 39-digit zero-padded decimal (`Store::format_log_key` = `format!("{value:0width$}", width = LOG_KEY_LEN)`, `LOG_KEY_LEN` renamed from `MAX_LOG_KEY_LEN`), so every key rendered by the portal has identical length; this supersedes the 2026-07-27 v8 note above (v8 stored unpadded 1-39 digit keys). `LogKeyDecodeMode` now has three variants and two predicates instead of `matches!` checks at call sites: `accepts_legacy_bytes()` (only `UpgradeV7`, raw 16-byte UUIDs) and `requires_padded_decimal()` (only `Current`); `UpgradeV7`/`UpgradeV8` decode leniently (1..=39 digits, leading zeros allowed) and re-emit padded, and `schema.rs` selects them with `match version { Some(7) => UpgradeV7, Some(8) => UpgradeV8, _ => Current }`. Key lesson for future format migrations: `rebuild_indexes` in `store/maintenance.rs` only rewrote entries whose `DecodedLogKey` was `Legacy`, so a pure re-encoding (same variant, different bytes) would NOT have been persisted; the trigger is now the byte comparison `key.as_bytes() != value.value()` (`stale_keys`), which covers both legacy conversion and padding normalization and drops the `DecodedLogKey` import there. The strict-mode round-trip check stays but is only meaningful for `Current` (`padded && canonical.as_bytes() != encoded`), since a lenient decode intentionally differs from its stored bytes.

- 2026-07-27: `IntentRuntime::check_repeat_invocation` (`server/intent/runtime.rs`) no longer keys its repeat counter on the raw `invocation.input`; the new private `repeat_invocation_key` + `canonical_value` pair drops the TOP-LEVEL server-injected `purpose` (`server/relay/prompt.rs` injects it into every execution tool schema and marks it required) and canonically re-serializes the rest. `purpose` is free text the model rewrites every call, so the old key never repeated and the `RepeatInvocation` guard could not fire. Two constraints shaped the implementation. (1) `marix-common` enables serde_json `preserve_order`, and Cargo unifies features workspace-wide, so `Map` is insertion-ordered EVERYWHERE and `to_string(&Value)` does NOT sort -- proven in a scratch crate: `from_str::<Value>(r#"{"url":"u","b":1,"a":2}"#)` re-serializes verbatim as `{"url":"u","b":1,"a":2}`. Sort explicitly (`entries.sort_unstable_by(|left, right| left.0.cmp(&right.0))`), the same precedent as `RelayRuntime::call_descriptor`; do not use `Map::sort_keys()`. (2) `common/external/serde_json.rs` exports only `Error, Value, from_slice, from_str, from_value, json, to_string, to_string_pretty, to_vec` -- NOT `Map`. Recursive canonicalization therefore matches on `Value::Object(members)` / `Value::Array(items)` and rebuilds through `.collect()` (`Map: FromIterator<(String, Value)>`), so the `Map` type is never named and the wrapper needs no new export. Note `marix-server`'s Cargo.toml has no direct `serde_json` dependency: `serde_json::` in that file resolves ONLY through `use marix_common::external::*;`, so this change also retired the file's long-standing unused-glob-import warning.- 2026-07-27 (follow-up): The `ToolCallFraming::Background` arm in `server/relay/prompt.rs` `append_tool_calls` now emits `[**COMPLETED CALLS**]:` (colon, no module text), while `Completed` emits `[**COMPLETED CALLS**]\n` + `prompt/module/CompletedCalls.prompt`. The asymmetry is deliberate: `CompletedCalls.prompt` says "for the [CURRENT TASK]", which is false for ancestor history, so the `Background` arm must never render it. The variant names stay `Background`/`Completed` (they name the caller, not the heading). `[**BACKGROUND**]` no longer exists anywhere under `src/`; `[BACKGROUND CONTEXT]` survives in exactly two places - the outer ancestor block header at `prompt.rs:195` and option 2 of `WorkflowPolicy.prompt`. In `WorkflowPolicy.prompt` only option 1 carries the `[**COMPLETED CALLS**]` cross-reference; the `FIRST: check ...` preamble line and the `ONLY when ... do not yet answer` rewording of option 4 were reverted as over-instruction. That file is CRLF, UTF-8 no BOM, 11 lines, with a significant trailing space at the end of line 1 - edit it byte-wise, not via line-rewriting helpers.

- 2026-07-27 (follow-up 2): `ToolCallFraming` is GONE from `server/relay/prompt.rs`; `append_tool_calls` is back to `(prompt, intent, separator) -> Result<(), String>` (still fallible: it loads `prompt/module/CompletedCalls.prompt`) and unconditionally emits `[**COMPLETED CALLS**]\n` + the module sentence for BOTH callers (`plan_prompt` ancestors and `pending_intent_prompt`). The enum only survived the heading unification because `CompletedCalls.prompt` said "for the [CURRENT TASK]", which was false on the ancestor side; dropping those three words from the module text (now "These calls have already been executed and the results below are confirmed. Treat them as established facts.", 110 bytes, UTF-8 no BOM, CRLF, trailing newline) removed the last reason to branch. The ancestor side also lost its trailing colon, so both blocks are now byte-identical apart from their wrappers -- attribution lives entirely in those wrappers: `[BACKGROUND CONTEXT]` (prompt.rs ~line 195) and `[**CURRENT TASK**]`. General lesson: when two prompt branches converge on the same heading, check whether the remaining divergence is only a hardcoded scope phrase in the shared module text -- neutralizing the phrase usually deletes the branch. `Prompt::read` is a bare `fs::read_to_string` (no CRLF normalization), so a CRLF single-line module renders with its `\r\n` intact and `notice.trim_end()` is what strips it -- keep the `trim_end()`.

- 2026-07-27: Two model-visibility fixes for tool calls. (1) `server/relay/prompt.rs` `call_descriptor` now prefers the real argument core over the model-authored `purpose` up to `CORE_DESCRIPTOR_MAX_CHARS` (256, was a bare 32), so quoting/escaping differences between a failing and a succeeding retry stay visible in `[**COMPLETED CALLS**]`; the file had no consts before, so the new one sits right after the imports per the style rule (consts above the `// -- Private -- //` marker), not next to the marker. (2) `server/invocation/runtime.rs` `summarize_and_finish` short-circuits the `ToolCallSummarize` relay when ALL of: `output.len() <= SUMMARY_BYPASS_BYTES` (2048 bytes, not chars), `continuation_cursor.is_none()`, and `summaries` is still empty -- the last two together guarantee the output is a complete single-shot tool output, since the summarize path is what drives `request_continuation`. It pushes `output.trim()` (when non-empty) and calls `finish_summary`, mirroring the `on_summarize_update` success branch; whitespace-only output still falls back to `No Summary`. Two implementation constraints: the summaries guard must be `drop`-ed before `finish_summary` (std `Mutex` is non-reentrant and `finish_summary` re-locks it), and holding one `mut` guard for both the emptiness test and the push lets the non-bypass path still produce `previous_summaries` with a single lock acquisition and no clone on the bypass path. `SUMMARY_BYPASS_BYTES` must stay well under 8 KiB: `ExecutorCache` (`host/executor/cache.rs`) returns content whole at or below `CACHE_THRESHOLD_BYTES` (24 KiB) and otherwise emits a `CHUNK_BYTES` (16 KiB) chunk, so any later chunk of a chain exceeds 8 KiB -- different crates, so nothing enforces it at compile time. Bypassed output reaches the prompt verbatim: `StepRuntime` copies `InvocationResult.output` into `ToolCallResultDraft.result`, and `append_tool_calls`'s `single_line` only collapses REAL CR/LF, so JSON-escaped `\r\n` inside a tool's JSON payload survives unchanged.


## Host executor chunk cache (2026-07-27)

- `src/host/executor/cache.rs` overlaps consecutive continuation chunks: `try_cache` emits `content[..end]` but caches `content[start..]` with `start = end - CHUNK_OVERLAP_BYTES` (1 KiB), walked back to a char boundary by `chunk_start`. Net advance per chunk is `CHUNK_BYTES - CHUNK_OVERLAP_BYTES` = 15360 bytes, so the overlap must stay strictly below `CHUNK_BYTES` or the continuation chain never terminates.
- `ToolCallSummarize.prompt` and `ToolCallSummarizeWithCursor.prompt` are maintained in parallel; their shared instruction paragraphs must stay byte-identical (only the lead sentence and the trailing JSON-shape blocks legitimately differ). Generate both from one string and verify with a SHA-256 comparison of the shared lines.
- The summarize relay has no short-output bypass: every tool call goes through `ToolCallSummarize`. A bypass constant (`SUMMARY_BYPASS_BYTES`) was added and reverted the same day; do not reintroduce it.
- Prompt templates are UTF-8 without BOM, CRLF, one flowing paragraph per block, no bullet lists. `[System.IO.File]::ReadAllBytes` in PowerShell ignores `cd`; always pass absolute paths.
- 2026-07-27: Both `ToolCallSummarize[WithCursor].prompt` templates are now ordered header-first for provider prompt-cache reuse: line 1 `[TOOL CALL]`, line 2 the fixed "just completed" sentence, line 3 lead + shared instruction run, line 4 the JSON-shape line, one blank line, then the variable payload (`{{#pre_chunk}}[TOOL: {{#tool}}]`, fenced `{{#output}}`, plus `[CONTINUATION CURSOR] {{#continuation_cursor}}` in the cursor variant only). Every byte before the first placeholder is a stable shared prefix, so ALL static instruction text must stay above `{{#pre_chunk}}`; never append prose after the payload again. Only two things legitimately differ between the pair: the lead clause (`Summarize the output below` vs `Summarize only the current chunk below`) and the trailing JSON-shape line; the run from `Copy concrete values verbatim:` through `so the JSON stays valid.` is 282 bytes, sha256 f0caa1dd..., and must stay byte-identical -- generate it once and diff the raw bytes, not the rendered text. Since the cursor is now printed BELOW the instructions, the cursor variant says "the cursor value shown below"; do not revert to "provided above".
- 2026-07-27 (follow-up 3): `prompt/module/CompletedCalls.prompt` is now 197 bytes (UTF-8 no BOM, ONE line, exactly one CRLF at EOF): the established-facts sentence plus " NEVER call same tool with previous same argument again if their results do not expire." It stays parameter-free (no `{{#...}}`) and attribution-neutral because `RelayRuntime::append_tool_calls` renders it for BOTH the ancestor block and `[**CURRENT TASK**]`. Keep it single-line: `render_prompt(...).trim_end()` only strips the trailing CRLF, so any interior newline would land verbatim under the `[**COMPLETED CALLS**]\n` heading. Edit this file byte-wise via `[System.IO.File]::WriteAllBytes` with `UTF8Encoding($false)` — PowerShell line helpers add a BOM and/or extra newlines; verify with `git --no-pager diff --numstat` showing exactly `1 1`.
- 2026-07-28: `PromptProfile::compose_messages` assigns roles by stable prompt position: the request system text is system, `prompts[0]` (WorkflowPolicy) is system, and every remaining context prompt is user. Preserve the empty-prompts path with `split_first()` so a request containing only the system text remains valid.
- 2026-08-01: Production Candidate 007 keeps a single decision stage and the existing workflow-tool merge unchanged. Its decision boundary is split across `prompt/template/System.prompt`, `prompt/template/WorkflowPolicy.prompt`, `prompt/module/CompletedCalls.prompt`, and private rendering strings in `server/relay/prompt.rs`: ancestor context is explicitly reference-only, while every current-task prompt starts with `[**END BACKGROUND CONTEXT**]` even when no ancestors exist. Dynamic ancestor goals, plans, and call outputs remain inserted without escaping, so they can spoof structural headings; treat that as an existing prompt-injection boundary if future hardening is requested.
- 2026-08-01: Prompt Benchmark candidate-016 maps directly to the production System, WorkflowPolicy, BackgroundTaskContextHeader, CurrentTaskContextHeader, and CompletedCalls templates; only `{{system}}`/`{{goal}}` become production `{{#system}}`/`{{#goal}}`. Keep each template UTF-8 without BOM, CRLF, and exactly one terminal CRLF. `server/relay/prompt.rs` continues to call `trim_end()` on module text before adding its existing separators, so no loader change is needed.
- 2026-08-01: Telemetry message copying is centralized in private `copyRecordMessage` within `server_telemetry/http/telemetry-message.js`; both Log Row context-menu copy and Format Message modal copy resolve the full record through `exactRecord`, copy raw `record.message` through `writeClipboardText`, and share the same toast and error handling.
- 2026-08-03: Benchmark candidate-029 promoted into production prompts with three single-line edits: `WorkflowPolicy.prompt` line 4 gained "A result reporting that a requested value was not found, not provided, or unavailable leaves that requirement unmet."; `module/CurrentTaskContextHeader.prompt` line 5 gained the "MUST call the one ordinary tool that obtains the missing values" + "a different URL, path, or query of the same source is not a repeat" pair; `module/CompletedCalls.prompt` (still ONE line, now 579 bytes) was rewritten to a requirement-by-requirement check that keeps `NEVER verify, repeat, or re-plan it` only inside the "Only when every Goal requirement is covered" clause. Root defect: absent value treated as satisfied, back-filled from the parent step, and the old unconditional never-repeat clause made re-fetching the named source feel forbidden. CompletedCalls text must stay attribution-neutral because `RelayRuntime::append_tool_calls` reuses it for BOTH ancestor and current-task blocks.
- 2026-08-03: `WorkflowPolicy.prompt` is 18 CRLF-terminated lines (not 11 as sometimes quoted), `module/CurrentTaskContextHeader.prompt` is 5, `module/CompletedCalls.prompt` is 1. To port a benchmark candidate byte-safely on Windows, use `[System.IO.File]::ReadAllText` + `String.Replace` + `WriteAllText` with `New-Object System.Text.UTF8Encoding($false)`; a within-line replace leaves every CRLF untouched. Prove the port by normalizing the candidate JSON's `{{system}}`/`{{goal}}` to `{{#system}}`/`{{#goal}}`, converting file CRLF to LF, `TrimEnd()`ing both sides, and comparing with `-ceq`; also assert the byte delta equals the inserted sentence length.
